apiVersion: v1
kind: Service
metadata:
  name: users-api-service
  namespace: default
  labels:
    app: users-api
spec:
  type: LoadBalancer
  selector:
    app: users-api
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
